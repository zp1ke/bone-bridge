# Bone Bridge appwrite

Manage appwrite with its [CLI tool](https://appwrite.io/docs/tooling/command-line/installation).

## Configuration

Copy `appwrite_sample.json` to `appwrite.json`.
Replace *PROJECT_ID*, *DATABASE_ID* and *COLLECTION_ID* with its real values.
